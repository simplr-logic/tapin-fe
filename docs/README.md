# docs

Reference and planning docs that aren't needed on every session start (unlike
`CLAUDE.md`, which stays at the repo root so Claude Code auto-loads it).

| File                                               | Purpose                                                                   |
| -------------------------------------------------- | ------------------------------------------------------------------------- |
| [`ARCHITECTURE.md`](ARCHITECTURE.md)               | Library and logic reference — stack, state, drag-and-drop, treemap layout |
| [`FE_LOGIN_FLOW_TASKS.md`](FE_LOGIN_FLOW_TASKS.md) | Login/account-linking backlog against `Klong_Login_Flow.docx`             |
